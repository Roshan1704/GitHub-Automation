browser=firefox
headless=true
stripe.base.uri=https://api.stripe.com
stripe.dashboard.url=https://dashboard.stripe.com/test
stripe.secret.key=${STRIPE_SECRET_KEY}
stripe.ui.email=${STRIPE_UI_EMAIL}
stripe.ui.password=${STRIPE_UI_PASSWORD}
api.retry.count=3
webhook.endpoint.secret=${STRIPE_WEBHOOK_SECRET}
